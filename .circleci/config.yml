version: 2.1

executors:
  base_bash:
      docker:
        - image: cimg/base:2023.04
      shell: /bin/bash

jobs:
  test:
    executor: base_bash
    steps:
      - run:
          command: echo $0
      - run: |
          echo $0

workflows:
  setup-workflow:
    jobs:
      - test
