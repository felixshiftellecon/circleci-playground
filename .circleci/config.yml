version: 2.1

orbs:
  aws-cli: circleci/aws-cli@3.1

jobs:
  test:
    docker:
      - image: cimg/base:2023.04
    shell: /bin/sh
    steps:
      - run:
          command: echo $0
          shell: /bin/bash
      - run: |
          echo $0

workflows:
  setup-workflow:
    jobs:
      - test
