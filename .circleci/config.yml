version: 2.1

# Config policy fixture: literal circleci/path-filtering pin (CPM sees semver).
# Branch: cpm-pf-2.0.0

orbs:
  path-filtering: circleci/path-filtering@2.0.0

executors:
  base:
    docker:
      - image: cimg/base:stable

jobs:
  policy-fixture-noop:
    executor: base
    resource_class: small
    steps:
      - run: echo "path-filtering orb literal pin 2.0.0"

workflows:
  policy_fixture:
    jobs:
      - policy-fixture-noop
