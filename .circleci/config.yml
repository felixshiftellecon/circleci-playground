version: 2.1

orbs:
  ios: nextraq/ios@1.0.7

jobs:
  job_a:
    executor: ios/macos
    steps:
      - checkout
      - save_cache:
          key: v1-nothing
          paths:
            - i_dont_exsist/
      - restore_cache:
          key: v1-nothing

workflows:
  job-workflow:
    jobs:
      - job_a