version: 2.1

orbs:
  aws-cli: circleci/aws-cli@3.1

jobs:
  test:
    macos:
      xcode: "14.2.0"
    steps:
      - run:
          command: echo $SHELL
          shell: /bin/bash --login -eo pipefail 
      - run: |
          echo "$SHELL"

workflows:
  setup-workflow:
    jobs:
      - test
