version: 2.1

executors:
  base:
    docker:
      - image: cimg/base:stable

jobs:
  job_a:
    machine:
      image: ubuntu-2004:2024.01.2
    resource_class: 2xlarge+
    steps:
      - run: echo "Don't run me"

  job_b:
    macos:
      xcode: "15.4.0"
    resource_class: m2pro.large
    steps:
      - run: echo "Don't run me"

workflows:
  workflow-a:
    jobs:
      - job_a
      - job_b