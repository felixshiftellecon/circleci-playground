version: 2.1

executors:
  base:
    docker:
      - image: cimg/base:stable

jobs:
  job_a:
    executor: base
    steps:
      - checkout
      - run: echo hello 1
  job_b:
    macos:
      xcode: "16.4.0"
    resource_class: m4pro.large
    steps:
      - checkout
      - run: echo hello 2

workflows:
  job-workflow:
    jobs:
      - job_a
      - job_b