version: 2.1

jobs:
  m2-latency:
    macos:
      xcode: 16.4.0
    resource_class: m2pro.medium

    steps:
      - run: curl -o /dev/null https://denisl-s3-latency.s3.us-east-1.amazonaws.com/FreeBSD-14.3-RELEASE-arm64-aarch64-bootonly.iso

  m4-latency:
    macos:
      xcode: 16.4.0
    resource_class: m4pro.medium

    steps:
      - run: curl -o /dev/null https://denisl-s3-latency.s3.us-east-1.amazonaws.com/FreeBSD-14.3-RELEASE-arm64-aarch64-bootonly.iso

workflows:
  my-workflow:
    jobs:
      - m2-latency
      - m4-latency
