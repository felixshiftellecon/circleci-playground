version: 2.1

executors:
  base:
    macos:
      xcode: "16.4.0"
    resource_class: m4pro.large

jobs:
  job_a:
    executor: base
    steps:
      - checkout
      - run: echo hello john
  job_b:
    executor: base
    steps:
      - checkout
      - run: echo hello 2

workflows:
  job-workflow:
    jobs:
      - job_a
      - job_b