version: 2.1

executors:
  base:
    docker:
      - image: cimg/base:stable

jobs:
  job_a:
    executor: base
    environment:
      CODEPUSH_DEPLOYMENT_NAME: pr$CIRCLE_PULL_REQUEST
    steps:
      - run: echo $CODEPUSH_DEPLOYMENT_NAME

workflows:
  workflow-a:
    jobs:
      - job_a