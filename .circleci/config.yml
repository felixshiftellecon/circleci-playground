version: 2.1

orbs:
  aws-cli: circleci/aws-cli@3.1

jobs:
  test:
    machine:
      image: ubuntu-2004:current
    steps:
      - run:
          command: |
                    echo $0
                    ls -l $(which $0)
          shell: /bin/sh
      - run: |
          ls -l $(which $0)

workflows:
  setup-workflow:
    jobs:
      - test
