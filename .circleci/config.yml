version: 2.1

orbs:
  ruby: circleci/ruby@2.0.0

jobs:
  build:
    machine:
      image: ubuntu-2004:2022.07.1
    resource_class: large
    steps:
      - checkout

      - ruby/install:
          version: '2.7'
      
      - run: ruby -v

workflows:
  run-jobs:
    jobs:
      - build