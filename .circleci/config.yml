version: 2.1

jobs:
  buildah:
    docker:
      - image: gcr.io/kaniko-project/executor:debug
    steps:
      - checkout
      - run:
          name: Building-a-container
          command: |
            /kaniko/executor \
                --context "$(pwd)" \
                --dockerfile "$(pwd)/Dockerfile"

workflows:
  buildah:
    jobs:
      - kaniko