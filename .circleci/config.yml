version: 2.1

executors:
  base:
    docker:
      - image: cimg/base:stable
    resource_class: medium

jobs:
  job_a:
    executor: base
    steps:
      - checkout
      - run: echo "Running job a"
      - run: cat path_a/README.md

workflows:
  oauth_triggered_workflow:
    when: 
      not: << pipeline.event.name >>
    jobs:
      - job_a
  github_app_triggered_workflow:
    when: << pipeline.event.name >> == "push"
    jobs:
      - job_a