version: 2.1

jobs:
  test:
    docker:
      - image: cimg/base:2023.04
    steps:
      - run: |
          echo "export CODEPUSH_DEPLOYMENT_NAME=pr${CIRCLE_PULL_REQUEST##*/}" >> "$BASH_ENV"
          source "$BASH_ENV"
          echo $CODEPUSH_DEPLOYMENT_NAME

workflows:
  setup-workflow:
    jobs:
      - test