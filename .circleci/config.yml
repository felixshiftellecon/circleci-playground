version: 2.1

executors:
  base:
    docker:
      - image: cimg/base:stable

jobs:
  job_a:
    executor: base
    steps:
      - run: echo "Hello John

workflows:
  job-workflow:
    jobs:
      - job_a
