version: 2.1

setup: true

orbs:
  node: circleci/node@7.2.1

workflows:
  setup-workflow:
    jobs:
      - node/run:
          bun-run: build
          executor:
            name: node/default
      - node/run:
          bun-run: build
          executor:
            name: node/default