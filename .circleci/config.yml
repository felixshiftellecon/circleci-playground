version: 2.1

jobs:
  job_a:
    machine:
      image: ubuntu-2004:current
    resource_class: medium
    steps:
      - checkout
      - save_cache:
          key: v1-nothing
          paths:
            - i_dont_exsist/
      - restore_cache:
          key: v1-nothing

workflows:
  job-workflow:
    jobs:
      - job_a