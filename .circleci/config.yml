version: 2.1

# Config policy fixture: literal circleci/path-filtering pin (CPM sees semver).
# Branch: cpm-pf-1

orbs:
  path-filtering: circleci/path-filtering@1

executors:
  base:
    docker:
      - image: cimg/base:stable

jobs:
  policy-fixture-noop:
    executor: base
    resource_class: small
    steps:
      - run: echo "path-filtering orb literal pin 1"

workflows:
  policy_fixture:
    jobs:
      - policy-fixture-noop
