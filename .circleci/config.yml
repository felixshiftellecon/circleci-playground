version: 2.1

executors:
  base:
    docker:
      - image: cimg/base:stable

jobs:
  job_a:
    executor: base
    steps:
      - checkout
      - run:
          command: exit 1 || echo Im a failure
          background: true
      - run: echo "Running job a"

workflows:
  workflow-a:
    jobs:
      - job_a