version: 2.1

executors:
  base:
    docker:
      - image: cimg/base:stable

jobs:
  job_a:
    executor: base
    steps:
      - run: circleci-agent step halt
  job_b:
    executor: base
    steps:
      - run: echo "running job b"

workflows:
  workflow-a:
    jobs:
      - job_a
      - job_b