version: 2.1

orbs:
  aws-cli: circleci/aws-cli@3.1

jobs:
  test:
    macos:
      xcode: "14.2.0"
    steps:
      - run:
          command: echo $0
          shell: /bin/sh 
      - run: |
          echo "$0"

workflows:
  setup-workflow:
    jobs:
      - test
