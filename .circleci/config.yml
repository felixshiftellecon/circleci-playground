version: 2.1

executors:
  base:
    docker:
      - image: cimg/base:stable
    resource_class: medium

jobs:
  job_a:
    executor: base
    steps:
      - checkout
      - run: echo "Running job a"
      - run: cat path_a/README.md

workflows:
  oauth_triggered_workflow:
    when: 
      not: << pipeline.trigger_type >> = "github_app"
    jobs:
      - job_a
  github_app_triggered_workflow:
    when: << pipeline.trigger_type >> = "github_app"
    jobs:
      - job_a